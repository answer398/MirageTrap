# MirageTrap Frontend

Vue 3 前端入口，包含控制层绑定登录、态势大屏和管理控制台。

## 命令

```bash
npm install
npm run dev
npm run build
npm run preview
```

## 默认地址

- 开发服务：`http://127.0.0.1:15173`
- 控制层默认地址：`http://127.0.0.1:15000`

登录页会先测活并绑定控制层，再使用管理员账号登录。一个前端服务可以绑定多个控制层。
